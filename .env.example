VITE_SUPABASE_URL=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Web Push — VAPID public key (safe to expose; ships to every browser).
# Must match the VAPID_PUBLIC_KEY edge-function secret. A built-in fallback
# is used if this is unset, so it's optional.
VITE_VAPID_PUBLIC_KEY=
